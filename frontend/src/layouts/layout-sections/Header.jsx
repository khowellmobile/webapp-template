import classes from "./Header.module.css";

const Header = () => {
    return (
        <div className={classes.mainContainer}>
            <p>Header</p>
        </div>
    );
};

export default Header;
