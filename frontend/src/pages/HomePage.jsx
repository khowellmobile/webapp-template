import classes from "./HomePage.module.css";

const HomePage = () => {
    return (
        <div className={classes.mainContainer}>
            <p>Hello</p>
        </div>
    );
};

export default HomePage;
